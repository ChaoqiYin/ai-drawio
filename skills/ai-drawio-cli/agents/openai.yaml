interface:
  display_name: "AI Drawio CLI"
  short_description: "Run ai-drawio with minimal optional flags."
  default_prompt: "Use $ai-drawio-cli to operate the ai-drawio command and avoid non-required flags unless the user explicitly needs them."
